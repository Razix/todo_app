= simple_form_for [@todo, @document], :html => { :class => 'form-horizontal' } do |f|
  = f.input :document
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), todo_documents_path, :class => 'btn'
