= simple_form_for [@todo, @document], remote: true, html: { class: 'form-horizontal' } do |f|
  = f.input :document, label: 'Upload your document', input_html: { class: 'btn btn-file' }
  .form-actions
    = f.button :submit, class: 'btn-primary'
