= simple_form_for [@commentable, @comment], remote: true, html: { class: 'form-horizontal' } do |f|
  = f.input :content, label: 'Add comment', input_html: { rows: 10 }
  .form-actions
    = f.button :submit, class: 'btn-primary'
